<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-800">
    <div class="w-full h-full bg-zinc-800  p-2 text-white">
        <div class="flex justify-end"><a href="/logout"class="px-4 py-2 bg-red-500 rounded-md">logout</a></div>
        
        <div class="flex item-start gap-3">
        <div class=" w-20 h-20 rounded-full bg-blue-500 overflow-hidden">
            <img class="w-full h-full object-cover "  src="<%= user.profilepic %>" alt="">
        </div>
        <h3 class="text-3xl ">Hello,<%=user.name%></h3>
        </div>

        <h5 class="mb-5">you can create a post</h5>
        <form action="/post" method="post">
            <textarea class="w-1/3 resize-none bg-transparent p-2 outline-none  block border-2  border-zinc-500 rounded-md"  placeholder="What is in your mind ?" name="content" ></textarea>
            <input class="px-3 py-2 bg-blue-500 block rounded-md text-sm mt-2 w-50 "   type="submit" value="Create New Post" >
        </form>
        <div class="posts mt-10">
            <h3>Your Posts.</h3>
            <div class="postcontainer mt-3">
               <% user.post.reverse().forEach(function(p){%>
                <div class="post mb-4 w-2/3 p-5 border-2 rounded-md border-zinc-500">
                    <h4 class="text-blue-500 mb-3">@<%= user.username %></h4>
                    <p class="tracking-tighter"><%= p.content %></p>
                    <small><%= p.likes.length %> likes</small>
                    <div class="btns flex mt-1 gap-4">
                        <a class="text-blue-500"   href="/like/<%= p._id %>"><%= p.likes.length===0 ? "like" : "unlike" %></a>
                        <a class="text-yellow-500"   href="/edit/<%=p._id%>">edit</a>
                    </div>
                </div>
                <%})%>
            
            </div>
        </div>
    
    </div>
</body>
</html>